<nav class="navbar navbar-expand-lg navbar-light bg-white fixed-top">
    <div class="container">

        <!-- Brand -->
        <a class="navbar-brand fw-bold d-flex align-items-center gap-2" href="/listings">
            <i class="fa-solid fa-house-chimney"></i>
            <span>Roomza</span>
        </a>

        <!-- Mobile Toggle -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Links -->
        <div class="collapse navbar-collapse" id="mainNav">

            <!-- ✅ SEARCH BAR (Responsive) -->
            <div class="w-100 w-lg-auto d-flex justify-content-center">
                <form class="d-flex my-3 my-lg-0 w-100 w-lg-auto navbar-search" action="/listings" method="GET">

                    <input class="form-control rounded-pill px-4 me-2 search-input" type="search" name="search"
                        placeholder="Search stays by title / location / country..."
                        value="<%= (typeof search !== 'undefined') ? search : '' %>" aria-label="Search">

                    <button class="btn btn-primary rounded-pill px-4" type="submit">
                        <i class="fa-solid fa-magnifying-glass"></i>
                    </button>
                </form>
            </div>

            <ul class="navbar-nav ms-auto align-items-lg-center gap-lg-3">

                <li class="nav-item">
                    <a class="nav-link" href="/listings">Explore</a>
                </li>

                <% if (currentUser) { %>
                    <!-- Logged-in user -->
                    <li class="nav-item">
                        <a class="btn btn-primary px-4 rounded-pill" href="/listings/new">
                            Add Stay
                        </a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link text-danger" href="/logout">Logout</a>
                    </li>

                    <% } else { %>
                        <!-- Guest user -->
                        <li class="nav-item">
                            <a class="nav-link" href="/signup">Signup</a>
                        </li>

                        <li class="nav-item">
                            <a class="btn btn-outline-primary px-4 rounded-pill" href="/login">
                                Login
                            </a>
                        </li>
                        <% } %>

            </ul>
        </div>

    </div>
</nav>

<!-- ✅ spacing for fixed navbar -->
<div class="nav-space"></div>